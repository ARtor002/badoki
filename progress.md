# وضعیت پروژه بدوکی

آخرین به‌روزرسانی: ۲۰۲۶-۰۸-۲۹

اگر چت قطع شد، همین فایل را به چت بعدی بده.

---

## الان کجا هستیم؟

پروژه فول‌استک رزرو نوبت پزشک است:

- بک‌اند: `backend/` — Spring Boot 3.3، پکیج `ir.artor.badoki`، PostgreSQL
- اندروید: `android/` — Java + Material 3، پکیج `ir.artor.badoki`
- نقش‌ها: `PATIENT` / `DOCTOR` / `ADMIN`

شاخه کاری: `arena/01a04ee3-badoki`

---

## همین مرحله (جستجوی پزشکان) — انجام شد

باگ صفحه پزشکان: فلگ مشترک `metaLoaded` باعث می‌شد **فقط یکی** از ردیف‌های فیلتر (شهر یا تخصص) ساخته شود؛ هر بار هر کدام زودتر از API برمی‌گشت، همان نمایش داده می‌شد.

اصلاح‌ها:

- سه فیلتر مستقل و هم‌زمان (AND): **تخصص + شهر + بیمارستان/کلینیک**
- هر ردیف برچسب دارد تا با هم قاطی نشوند
- جعبه جستجو روی **نام، تخصص، شهر و بیمارستان** کار می‌کند
- API جدید: `GET /api/meta/hospitals` و پارامتر `hospital` در `GET /api/doctors`
- دکمه «پاک کردن فیلترها» و نمایش تعداد نتیجه
- تست بک‌اند: `DoctorRepositorySearchTest`

---

## قابلیت‌هایی که از قبل پیاده شده

- احراز هویت JWT + OTP ایمیل (ثبت‌نام، ورود، فراموشی رمز)
- چرخه نوبت: PENDING → CONFIRMED → COMPLETED / CANCELED
- انقضای خودکار نوبت‌های گذشته (`AppointmentScheduler`)
- اطلاع‌رسانی درون‌برنامه‌ای (پولینگ، بدون WebSocket)
- امتیاز و نظر بیماران
- بخش آموزش
- داشبورد جدا برای بیمار / پزشک / ادمین
- تقویم شمسی، ارقام فارسی، RTL، حالت تاریک

حساب‌های نمونه:

| نقش | ایمیل | رمز |
|---|---|---|
| بیمار | ali@example.com | 123456 |
| پزشک | doctor@example.com | 123456 |
| ادمین | admin@badoki.ir | admin123 |

---

## اجرا

```bash
# دیتابیس
cd backend && docker compose up -d

# بک‌اند
cd backend && mvn spring-boot:run
# بدون Postgres: mvn spring-boot:run -Dspring-boot.run.profiles=h2
```

اپ اندروید: پوشه `android` را در Android Studio باز کنید.
آدرس سرور در `android/.../api/ApiClient.java` فیلد `BASE_URL` است.

---

## گزارش

اسکریپت‌های گزارش (`make_report.py` و دیاگرام‌ها) در چت قبلی بودند و در این ریپو نیستند.
فصل‌های ۱ تا ۳ گزارش نوشته شده بود؛ فصل ۴ (ارزیابی) و ۵ (نتیجه‌گیری) مانده.

---

## کار بعدی پیشنهادی

پروژه از نظر جستجو و فیلتر پزشکان باید stable باشد. موارد اختیاری بعدی:

1. بیلد APK و یک دور تست دستی صفحه پزشکان (سه ردیف فیلتر با هم)
2. فصل ۴ و ۵ گزارش
3. در صورت نیاز: FCM برای نوتیفیکیشن سیستم‌عامل، پرداخت آنلاین
