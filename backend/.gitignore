# ===== بکاند (Spring Boot + Maven) =====

# خروجی بیلد Maven
target/

# دیتابیس فایل‌محور H2 (وقتی با پروفایل h2 اجرا می‌شود)
data/
*.mv.db
*.trace.db

# لاگ‌های اجرا
*.log

# فایل‌های IDE مخصوص کاربر (روی سیستم خودتان می‌مانند)
.idea/
*.iml
.vscode/
.settings/
.classpath
.project

# Maven wrapper خودکار تولید نمی‌شود؟ این را نگه دارید (کامیت می‌شود):
# .mvn/  و  mvnw  و  mvnw.cmd
# — این سه را کامیت کنید (وابستگی ندارند و تیم را از نصب Maven بی‌نیاز می‌کنند)
